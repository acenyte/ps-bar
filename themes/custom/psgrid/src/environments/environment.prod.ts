const apiVersion = 1;

export const environment = {
  production: true,
  apiVersion,
  api: `http://hydrastage09.aquawin.com/psbar/cadbury/api/${apiVersion}`,
  base: `http://hydrastage09.aquawin.com/psbar/`
};
